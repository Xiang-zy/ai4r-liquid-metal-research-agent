# 科学计算复现与数据接入（5.4.0）

## 三类输入各司其职

| 输入 | 用途 | 可独立复现的范围 |
|---|---|---|
| `papers.py` 合成记录 | 无 API 程序演示、回归测试 | 流程与产物，不作为文献证据 |
| `data/submitted_baseline.json` | 固定的 25 个整理锚点及计算参数 | 原提交数值基线的候选、Pareto 前沿、来源波动、反事实、等预算对照 |
| `data/online_observations.json` | 50 篇保存结果中通过规则校验的归一化数值 | 5.4.0 新增观测接入后的预测、跨来源对照和 ERCPD 数值结果 |

`online_expected.json` 保存版本化预期结果及两个输入文件的 SHA-256。复现脚本不调用 MiniMax 或 Sciverse，不读取本地凭据，不需要私有正文。哈希用于检测输入变化，不代表已完成原始文献审计。

## 实际回放结果

本次复用 50 张原知识卡片，对 P031、P029、P027、P030、P057 的已保存文本做 5 次定向抽取，不重复整批检索。

原 75 条数值与新增抽取合计 87 条候选，经统一规则处理：20 条有效物性，62 条非目标物性或非材料量，3 条重复，2 条单位或数值不兼容。有效物性包括熔点 4 条、电导率 4 条、表面张力 2 条、密度 1 条、黏度 1 条、热导率 1 条、应变 6 条、应变系数 1 条。复合材料性能可保留在知识记录中，但不进入块体合金代理。

仅下列 8 条物性具备可核对的质量配比并进入代理；一行是一项物性观测，不等于一项独立实验：

| 来源标识 | Ga/In/Sn（wt%） | 接入物性 |
|---|---|---|
| P031 | 75/25/0 | 熔点 15.5 °C |
| P027 | 77/23/0 | 熔点 15 °C；密度 6.25 g/cm³；表面张力 624 mN/m；电导率 3.4×10⁶ S/m |
| P057 | 68.5/21.5/10 | 黏度 0.0025 Pa·s；表面张力 600 mN/m；电导率 3.4×10⁶ S/m |

P029、P030 的已保存配比段落未明确百分比的质量基准，不能仅凭合金名称补猜；其物性保留，但不加入质量配比代理。多个属性或多个来源可能重复转述同一原始实验，来源组不代表已证实的独立实验。

## 关系输出与证据等级

- 同来源、同条件观测关联：当前批次 0 条；不能凭代码补造缺失的成组实验。
- 跨来源描述性对照：3 组，分别涉及熔点、电导率、表面张力；显示不同配比的已有物性，不分离元素、来源和测试条件的影响。
- 跨来源三配比拟合：当前 0 条；不对只有两个配比的属性输出拟合趋势。
- 模型反事实：4 条，分别为 Sn 增减下的熔点与电导率变化；同时输出来源波动和方向一致性，只作为计算假说。

新版本会区分以上结果并在 JSON/HTML 中展示；观测关联仍要求额外的同条件配比对照数据。原先按材料名称输出固定科学叙述的规则分支已移除。

## 代理接入与版本区别

代理为按属性的距离反比插值，不是另行训练的神经网络。新增观测是插值支撑点；缺失属性保持缺失，质量配比必须有文本证据，原文数字和单位须通过校验。引用来源留一会移除对应的全部物性观测，公开复现同时检查移除数量和预测变化。

新增 8 条观测与 25 个整理锚点组成 33 个支撑记录、15 个来源组。来源信息未完成逐项原始论文审计，测试条件也未统一；当前代理按组成汇总，不能解释为已控制温度或样品制备的实验模型。

原固定锚点基线的风险调整候选是 Ga/In/Sn=65/22.5/12.5 wt%；5.4.0 接入新观测后的候选为 67.5/22.5/10 wt%。这是输入改变后的计算结果，不声称性能提升，也不替换已提交的代码包或报告。

## 本地继续处理

```bash
# 全部离线回放；输出含本地证据，不应直接发布
python3 scripts/replay_cards.py --cards /path/to/knowledge_cards.json --output outputs/local-replay

# 仅为指定文献调用 MiniMax；其余数值评估离线进行
python3 scripts/replay_cards.py --cards /path/to/knowledge_cards.json --output outputs/local-refinement --refine-ids P031,P027

# 显式导出数值白名单，发布前仍需检查
python3 scripts/export_numeric_observations.py --cards outputs/local-refinement/knowledge_cards.json --output outputs/review-numeric.json
```

可通过本地环境变量 `MINIMAX_TIMEOUT_SECONDS` 调整请求超时。输出目录中的知识卡片、证据引文、请求缓存和执行日志不属于公开数据。公开快照支持上述数值阶段复现，不包含原始检索服务响应和第三方全文；代码许可不重新授权第三方文献。
