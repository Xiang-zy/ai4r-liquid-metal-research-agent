# LM-LitAgent 5.3.1 验证摘要

## 自动化测试

发布版在 Python 3.12.13 / macOS 环境中通过 **60 项自动化测试**。测试覆盖完整离线 CLI、JSON/HTML 产物生成、输入安全、接口兼容性、知识抽取、证据融合、优化器和构效分析。

```bash
python3 -m unittest discover -s tests -v
```

主要验证内容包括：

- MiniMax 与 Sciverse 请求及响应格式；
- 熔点、电导率、应变等数值与单位的上下文识别；
- 材料名称、测试条件和证据位置的规范化；
- DOI 去重、来源计数和知识冲突识别；
- BO 预测、统一预算限制和搜索域约束；
- ERCPD 来源留一评估、风险调整及 Pareto 候选生成；
- 反事实组成守恒、方向一致性和弱效应标记；
- 缓存隔离、异常响应处理和结果目录保护。

## 可复现性检查

两组不同 `PYTHONHASHSEED` 的完整离线运行在排除时间字段后，知识卡片、研究空白和路线 A 结果保持一致。ERCPD 的同实现重算与独立算术聚合共 **13 项检查全部通过**。

离线演示不会发起外部请求：

```bash
python3 run.py --offline --max-papers 5
```

## 等预算比较

Random、GA 和 BO 使用共同组成域、相同目标函数、5 个固定种子，以及每个方法每个种子 20 次目标评估。当前代理协议下的平均 fitness 为：

| 方法 | 平均 fitness | 每种子目标调用数 |
|---|---:|---:|
| Random | 0.820840 | 20 |
| GA | 0.808560 | 20 |
| BO | 0.848600 | 20 |

运行命令：

```bash
python3 scripts/equal_budget_benchmark.py --output outputs/equal-budget.json
```

输出文件包含全部种子、逐次搜索轨迹、候选组成和汇总统计。

## 在线工作流检查

MiniMax 文本生成、Sciverse 检索及正文读取接口均完成实际连通检查。50 篇正常模式工作流生成：

- 50 张知识卡片；
- 75 条结构化数值记录；
- 7 个研究 Gap 候选；
- 7 项最终工作流产物。

路线 A 在该批次识别出 2 条关键属性记录。当前版本已完整保留回退、服务状态和构效输入统计，便于继续提高关系抽取覆盖率。

## 结果定位

系统输出属于文献数据和代理模型支持的计算候选，适合用于研究方向筛选、证据复核和后续实验设计。正式科学结论仍应结合原始文献与实验验证。
