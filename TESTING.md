# 5.3.1 验证摘要

2026-09-03，Python 3.12.13，同一台 macOS 机器。发布版 60 项测试通过，包括
完整离线 CLI、全部 JSON/HTML 输出、输入转义及 59 项共享检查；参赛包因保留
不同的历史演示输入、不包含发布版专用样例测试，计 59 项。

修复范围：BO后验均值；Galinstan误识别；正则温度/百分比语义；单位与条件
混用；不实证据等级；重复DOI；错误响应隐私；缓存账号隔离；结果目录覆盖；
Gap缺少描述导致HTML崩溃。GP均值现使用观测y，并有插值及线性缩放回归测试。

两次不同PYTHONHASHSEED的完整离线运行在排除时间字段后，知识卡片、Gap及
路线A结果一致。ERCPD同实现重算与独立算术聚合共13项检查通过；不是独立
模型、原始文献或实验验证。25个锚点全部仍待原文逐项审计。

等预算对照：每方法每种子20次目标评估，共同域50≤Ga≤95、In/Sn非负且
组成总和100；种子42、123、456、789、1024。Random20均值0.820840，
有界GA20均值0.808560，BO20均值0.848600。每组完整轨迹由脚本输出，未做
显著性检验、不宣称普遍优势或实测材料收益。新BO seed42结果0.8543替代
错误旧实现的0.7714。

真实接口检查中，MiniMax-M3 完成文本生成，Sciverse 检索和最小正文读取通过。
50 篇正常模式工作流完整结束，生成 50 张知识卡片、75 条数值记录、7 个 Gap
候选和 7 项最终产物。运行中有 20 张规则回退卡片及 1 次检索最终失败；路线 A
形成 2 条关键属性记录，尚未形成关系输出或新抽取代理点，因此不标为严格在线
闭环通过。完整原文、缓存和在线日志仅保存在本地，不纳入代码仓库；用量和服务
可用性仅反映测试时刻。

测试不等于100%覆盖，也不保证没有其他问题。原始文献真实性、新颖性、
第三方再分发许可、其他操作系统与Python版本不在此次已验证范围。
