# LM-LitAgent 5.4.0 验证摘要

## 自动化测试

发布版在 Python 3.12 / macOS 环境中通过 **80 项自动化测试**。测试覆盖完整离线 CLI、JSON/HTML 产物生成、输入安全、接口兼容性、知识抽取、证据融合、优化器和构效分析。

```bash
python3 -m unittest discover -s tests -v
```

主要验证内容包括：

- MiniMax 与 Sciverse 请求及响应格式；
- 熔点、电导率、应变等数值与单位的上下文识别；
- 材料名称、测试条件和证据位置的规范化；
- DOI 去重、来源计数和知识冲突识别；
- BO 预测、统一预算限制和搜索域约束；
- ERCPD 来源留一评估、风险调整及 Pareto 候选生成；
- 反事实组成守恒、方向一致性和弱效应标记；
- 缓存隔离、异常响应处理和结果目录保护。

## 可复现性检查

5.4.0 新增 20 项回归测试，覆盖属性别名、TeX 数字、单位前缀、质量配比证据、缺失性能不补造、复合物排除、重复去除、新增来源留一、观测关联条件、数值导出隐私和公开数值快照重算。完整离线 CLI 仍检查零外部请求、版本和源码哈希、HTML 转义及产物生成。

固定锚点基线重算 **6 项比对全部通过**；新增观测数值快照重算 **11 项比对全部通过**，包括候选、反事实、来源移除数量、预测变化、跨来源对照和输入哈希。命令与输入范围见 [REPRODUCIBILITY.md](REPRODUCIBILITY.md)。这些是确定性重放和回归检查，不是独立实验验证。

离线演示不会发起外部请求：

```bash
python3 run.py --offline --max-papers 5
```

## 等预算比较

Random、GA 和 BO 使用共同组成域、相同目标函数、5 个固定种子，以及每个方法每个种子 20 次目标评估。当前代理协议下的平均 fitness 为：

| 方法 | 平均 fitness | 每种子目标调用数 |
|---|---:|---:|
| Random | 0.820840 | 20 |
| GA | 0.808560 | 20 |
| BO | 0.848600 | 20 |

运行命令：

```bash
python3 scripts/equal_budget_benchmark.py --output outputs/equal-budget.json
```

输出文件包含全部种子、逐次搜索轨迹、候选组成和汇总统计。

## 保存的在线批次回放

在原 50 张知识卡片基础上，对 5 篇保存文本完成 MiniMax 定向抽取，再离线执行路线 A：

- 87 条候选数值，经处理保留 20 条有效物性；
- 12 条属于熔点、电导率、表面张力、密度和黏度；
- 8 条配比明确的物性进入共享代理和 ERCPD；
- 3 组跨来源配比—物性对照，4 条模型反事实趋势；
- 同来源、同条件的观测关联仍为 0，不将跨来源或模型输出充作实测规律。

公开数值回放不调用 API。定向抽取的原文与响应仅保存在本地，公开仓库提供去除引文的数值白名单。该验证不等同于重新完成 50 篇端到端在线检索或服务可用性保证。

## 结果定位

系统输出属于文献数据和代理模型支持的计算候选，适合用于研究方向筛选、证据复核和后续实验设计。正式科学结论仍应结合原始文献与实验验证。
