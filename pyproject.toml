[project]
name = "ai4r-liquid-metal-research-agent"
version = "5.4.0"
description = "Evidence-bound liquid-metal research agent with reproducible numeric snapshots"
requires-python = ">=3.10"
dependencies = []
license = "LicenseRef-LMLitAgent-NC-Reciprocal-1.0"
license-files = ["LICENSE"]

[tool.unittest]
start-directory = "tests"
